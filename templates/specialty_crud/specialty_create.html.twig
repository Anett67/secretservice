{% extends 'admin.html.twig' %}


{% block body %}

    <h1>{{ edit ? 'Modifier' : 'Ajouter' }} une spécialité</h1>

    <div class="row mb-5">

        <div class="col col-md-6">
            {{form_start(form)}}
                {{ form_widget(form) }}
                <input type="submit" class="btn btn-success btn-submit" value="Enregistrer">
            {{form_end(form)}}
        </div>
    
    </div>

{% endblock %}
